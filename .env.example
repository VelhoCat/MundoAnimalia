# Copia este archivo como .env y ajusta los valores.

# URL base de la API PHP (sin barra final).
VITE_API_URL=http://localhost/MundoAnimalia-funciones-sociales/backend

# "true" = datos mock en memoria (sin backend). "false" = API real PHP/MySQL.
VITE_USE_MOCK=false
